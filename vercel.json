{
  "version": 2,
  "buildCommand": "pnpm --filter @workspace/cosmos-realestate run build",
  "outputDirectory": "artifacts/cosmos-realestate/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
